spring.application.name=java-inventory-api
server.port=8081
# Actuator's health endpoint doubles as this app's /health for the demo —
# no need for a hand-written one when Spring already ships a correct one.
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
